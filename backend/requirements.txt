fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
lunar_python==1.4.8
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
# M2b:Apple App Store Server API 集成(JWT 签名 + JWS 验签 + transaction 查询)
# 用户 2026-07-18 同意引入(memory project_monetization_decisions.md 记录)
# 锁定 <2.0.0 防大版本 API break,苹果升级时手动 bump
app-store-server-library>=1.3.0,<2.0.0
# PR2.5:自家 JWT 签发/验证(HS256) + Apple Sign in ID Token 验签
# 用户 2026-08-10 同意引入(plan 中明确征求同意)
# Python JWT 标准库(50M+ downloads/month,MIT 协议,纯 Python)
pyjwt>=2.8.0,<3.0.0
# RS256 验签(Apple 公钥)需要 cryptography(pyjwt[_crypto] 隐含)
# venv 装 cryptography 在 Rosetta 上慢,用 --only-binary=:all: 装预编译 wheel
cryptography>=41.0.0

