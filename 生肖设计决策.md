# 生肖设计决策

2026-08-10 grill-me 锁定。生肖作为 onboarding 第一次反馈的"具体感知"瞬间 + 主屏低调常驻锚点。本文档是生肖模块的事实源(设计决策 + 资产清单 + 工程 checklist),与 `DESIGN.md` / `bazi-app-design-doc.md` 互为补充。

## 一句话

用户首次填完出生信息 → 过渡屏出现生肖印章图(细线图腾 + 圆环 + 朱砂光晕盖章动效)+ "辰·龙" 大字 + "乾造(男)·庚辰年(2000)" 次文字 → 用户主动点 CTA 进今日运势;之后 ChartHeader 嵌入"乾造(男)·庚辰年(2000)"文字低调常驻,ProfileView 命主卡放生肖图 + 今日 anchor。

## 决策矩阵(grill-me Q4-Q21)

| Q | 决策点 | 锁定 |
|---|---|---|
| 4 | 时序 | **(Z)** oneshot 仪式 + 低调常驻 |
| 5 | 发生位置 | **(b)** 提交后过渡屏(独立一屏) |
| 6 | 视觉主体 | **(3)** 生肖图形 silhouette |
| 7 | 风格 | 细线几何 + 图腾(原推荐印章后改 silhouette) |
| 8 | 视觉约束 | 细线(~1pt) / 黑墨 `#1C1C1C` / 圆环外框(~0.5pt) |
| 9a | 迭代 | 加图腾风格(v2 通过) |
| 9b | 构图策略 | **(iii)** 混合(正脸 4 / 侧脸 3 / 3-4 分面 4 / 盘绕 1) |
| 11 | 过渡屏布局 | **(β)** 印章 + 称谓 + 出生信息回显 |
| 12 | 主文字 | **(iii)** `辰 · 龙`(中点分隔) |
| 13 | 次文字 | `乾造(男) · 庚辰年(2000)`(双轨:命理 + 公历) |
| 14 | 结束机制 | 手动 CTA "查看今日运势" |
| 15 | 动效 | 盖章动效(`scaleEffect 0.8→1.05→1.0` + `opacity 0→1` + 朱砂 Circle stroke 扩散,~0.5s) |
| 16 | 常驻形态 | **(δ)** ChartHeader 文字嵌入 + ProfileView 命主卡双锚点 |
| 17 | 命主卡联动 | 加今日 anchor(宜/忌一句话,点击跳 `.dailyFortune` tab) |
| 18 | 暗色模式 | **(A)** 双版本图(`#1A1815` 暖墨底 + `#E8E0CC` 浅暖墨 stroke,呼应 `DESIGN.md` §Color 墨夜瓷釉) |
| 19 | 失败路径 | **(A)** 严格阻断 + retry(CLAUDE.md "错误显式传播"约束) |
| 20 | 编辑流 | **(B)** 严格防错(二次确认) + ProfileView"重置命盘"按钮 fallback |
| 21 | 图片体积 | **(C)** WebP 无损(27MB → 17MB,节省 35%) |

## 过渡屏 ASCII Mockup

```
┌─────────────────────────────┐
│                             │
│         ┌─────────┐         │
│        │           │        │  ← 圆环生肖图(1024pt 生成)
│        │  [龙图腾]  │        │     盖章动效:scale 0.8→1.05→1.0
│        │           │        │     朱砂光晕 #C33B3B 扩散一圈消散
│         └─────────┘         │
│                             │
│         辰 · 龙              │  ← Songti SC display 大字
│                             │
│   乾造(男) · 庚辰年(2000)    │  ← Songti SC subheadline
│                             │
│      ┌──────────────┐       │
│      │ 查看今日运势  │       │  ← 朱砂 CTA #C33B3B
│      └──────────────┘       │
│                             │
└─────────────────────────────┘
背景:#FDFCFA(亮)/ #1A1815(暗)
```

## ProfileView 命主卡 Mockup

```
┌──────────────────────────────────┐
│                                  │
│   ┌──────┐                       │
│   │ 龙图腾│  乾造(男) · 庚辰年(2000) │  ← 印章图 64pt + 主文字
│   │  64pt │  真太阳时:2000-05-03 14:30 │
│   └──────┘  出生地:广州             │
│                                  │
│   ──── hairline 0.5pt ────        │
│                                  │
│   今日 · 宜 静思 · 忌 冲动         │  ← 今日 anchor,点击跳今日 tab
│                                  │
└──────────────────────────────────┘
背景:#EBE3D0(亮)/ #25221D(暗)
```

## 资产清单

```
.tmp-zodiac-samples/
├── zodiac_01_rat.webp     380KB  ┐
├── zodiac_02_ox.webp      419KB  │
├── zodiac_03_tiger.webp   593KB  │
├── zodiac_04_rabbit.webp  682KB  │
├── zodiac_05_dragon.webp  678KB  │  Light 12 张,8.6MB 总
├── zodiac_06_snake.webp   711KB  │
├── zodiac_07_horse.webp   641KB  │
├── zodiac_08_goat.webp    650KB  │
├── zodiac_09_monkey.webp  633KB  │
├── zodiac_10_rooster.webp 650KB  │
├── zodiac_11_dog.webp     652KB  │
├── zodiac_12_pig.webp     555KB  ┘
├── dark/
│   └── zodiac_01..12_*.webp      Dark 12 张,11MB 总
├── contact_sheet.png              Light 12 张综合预览
└── compare_sheet.png              Light vs Dark 对比
```

构图分布:
- **正脸对称**:龙(05) / 虎(03) / 猴(09) / 猪(12)
- **侧脸 profile**:鼠(01) / 马(07) / 鸡(10)
- **3/4 分面**:牛(02) / 兔(04) / 羊(08) / 狗(11)
- **盘绕**:蛇(06)

## 工程 Checklist(按依赖顺序)

### 阶段 1:Assets 集成
- [ ] 在 `Assets.xcassets/Zodiac/` 建 12 个 image set(命名:`Zodiac_Rat`、`Zodiac_Ox`、... 大驼峰)
- [ ] 每个 image set 用 Xcode appearance set:light slot 放 light `.webp`,dark slot 放 dark `.webp`
- [ ] Xcode 编译验证

### 阶段 2:数据层
- [ ] 后端 `/api/bazi/calculate` response 加:
  - `year_branch_zodiac`(生肖中文字符串,如 `"龙"`)
  - `year_pillar_gan_zhi`(年柱干支,如 `"庚辰"`)
- [ ] iOS `BaziResponse` DTO 加对应字段 + decode 单测
- [ ] iOS 加 `ZodiacAnimal` enum 12 case,从 `year_branch_zodiac` → image asset name 映射

### 阶段 3:BirthFormView 防错 + 失败处理
- [ ] 提交前二次确认 sheet(`你的出生信息:2000-05-03 14:30 男 · 广州,确认?`)
- [ ] API 失败:alert `"网络异常,请重试"` + retry 按钮
- [ ] 3 次失败:alert `"请检查网络后重启 App"`
- [ ] 生肖字段缺失(开发期 bug):`fatalError` 上报,不 fallback

### 阶段 4:过渡屏 ZodiacRevealView(新文件)
- [ ] 新建 `ios/QiCompass/QiCompass/Features/Onboarding/ZodiacRevealView.swift`
- [ ] 布局:VStack[印章图 280pt + "辰·龙" Songti display 大字 + "乾造(男)·庚辰年(2000)" subheadline + 朱砂 CTA]
- [ ] 盖章动效:`.scaleEffect(0.8 → 1.05 → 1.0)` + `.opacity(0 → 1)` + 朱砂 Circle stroke 扩散(~0.5s 总时长)
- [ ] CTA action:切到 `.dailyFortune` tab
- [ ] `BirthFormView` 提交成功 segue 改为 `ZodiacRevealView`(替换原直接跳主 tab)

### 阶段 5:ChartHeader 改动
- [ ] `ChartHeaderView.swift:11-13` 主标字符串加 yearPillarLabel
- [ ] 新增 `yearPillarLabel: String` 计算:`"\(response.yearPillarGanZhi)(\(gregorianYear))"`

### 阶段 6:ProfileView 命主卡 + 重置
- [ ] `ProfileView.swift:24-41` 顶部加 `IdentityCard` view(在 List 之前或 Section 0)
- [ ] `IdentityCard`:HStack[印章图 64pt + VStack[主文字行 + 真太阳时行 + 出生地行] + hairline + 今日 anchor 行]
- [ ] 今日 anchor 点击 → 切到 `.dailyFortune` tab + 滚动到宜/忌
- [ ] 设置 section(line 96-110)加"重置命盘"按钮(`role: .destructive`,二次确认 alert)
- [ ] 重置 action:清空 SwiftData(`snapshotLinks` / `entitlements` / 解读缓存) + 重新走 onboarding

### 阶段 7:测试
- [ ] 12 生肖各跑一遍 onboarding(验证每个图正确显示 + 生肖文字匹配)
- [ ] Light / Dark 模式各跑一遍(切换 `colorScheme`)
- [ ] 失败路径:关网络测 retry + 3 次失败 alert
- [ ] 重置命盘流程(清空后重新 onboarding)

## 边界与约束

1. **后端权威**:生肖 + 年柱干支由后端 `lunar_python` 算,客户端只渲染不做推导(避免客户端算法分叉)
2. **CLAUDE.md 错误显式传播**:Q19 选严格阻断而非静默降级,生肖字段缺失视为 bug 直接 crash 上报
3. **v1 单用户**:不开放出生信息编辑(Q20)。用户输错只能"重置命盘"全部重来,不走 v2 多 snapshot 编辑流
4. **App size**:24 张 WebP 共 17MB,占 App Store cellular limit(200MB)8.5%,可接受
5. **生成资产外部依赖**:24 张图通过 gettoken.dev image gen API 生成,prompt 见 `.tmp-zodiac-samples/gen_all.py` / `gen_all_dark.py`(可重现)

## 相关文档

- `DESIGN.md` §Color——色板双值(墨夜瓷釉)
- `DESIGN.md` §iOS SwiftUI 落地计划——BaziTheme token
- `bazi-app-design-doc.md` §Open Questions §2026-08-01 grill-me 锁定段——onboarding B2 强制轻注册 + β 点击触发
- `命理引擎设计决策.md`——后端确定性算法边界
- `CLAUDE.md`——错误显式传播 / 不擅自加依赖 / Git commit 三段式约束
